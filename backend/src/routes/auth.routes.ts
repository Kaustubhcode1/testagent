import { Router } from 'express'; import { login, logout, validateSession } from '../controllers/auth.controller';  const router = Router();  router.post('/login', login); router.post('/logout', logout); router.get('/session', validateSession);  export const authRoutes = router;